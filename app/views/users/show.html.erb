<%provide(:title, 'show user')%>

<div class = "row" >
	<aside calss = "col-md-4">
		<section class="user_info">
			<h1>				
			<%= gravatar_for @user %>
			<%= @user.name %>
			<%= @user.email %>			
			</h1>
		</section>		
	</aside>
</div>
 